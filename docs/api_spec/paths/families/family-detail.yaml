get:
  tags: [Epic 2 - Users, Citizens, Officials]
  summary: Lihat detail KK beserta anggota
  operationId: getFamily
  security:
    - sanctumCookie: []
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
      example: 20
  responses:
    '200':
      description: Detail KK berhasil diambil
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                allOf:
                  - $ref: '../../schemas/families/families.yaml#/Family'
                  - type: object
                    properties:
                      members:
                        type: array
                        items:
                          $ref: '../../schemas/citizens/citizens.yaml#/Citizen'
    '401':
      $ref: '../../responses/common.yaml#/Unauthenticated'
    '403':
      $ref: '../../responses/common.yaml#/Forbidden'
    '404':
      $ref: '../../responses/common.yaml#/NotFound'
patch:
  tags: [Epic 2 - Users, Citizens, Officials]
  summary: Edit KK (No KK tidak bisa diubah setelah dibuat)
  operationId: updateFamily
  security:
    - sanctumCookie: []
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
      example: 20
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../schemas/families/families.yaml#/FamilyUpdateRequest'
  responses:
    '200':
      description: KK berhasil diperbarui
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../../schemas/families/families.yaml#/Family'
    '401':
      $ref: '../../responses/common.yaml#/Unauthenticated'
    '403':
      $ref: '../../responses/common.yaml#/Forbidden'
    '404':
      $ref: '../../responses/common.yaml#/NotFound'
    '422':
      $ref: '../../responses/common.yaml#/ValidationError'
